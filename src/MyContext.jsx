import { createContext } from 'react';

// Step 1: Create the context outside any component
const MyContext = createContext();

// Step 2: Export the context so it can be used in other components
export default MyContext;
