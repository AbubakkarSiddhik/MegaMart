import { createContext } from 'react';

//  Creating the context outside any component
const MyContext = createContext();

//  Exporting the context so it can be used in other components
export default MyContext;
